BARDBOT:
  move evaluation tool:
    -find a way to generate random positions and have them correctly evaluated
    -linear regressor to predict move eval
    -record api opening calls to local map for offline storage

960 castle breaks if rook and king start next to each other -> not a priority

randombot move selector and bot game api

DL model based on linear regression evaluation?

